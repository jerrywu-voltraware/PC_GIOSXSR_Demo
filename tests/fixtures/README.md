# Mobile protocol reference

`mobile_pru_protocol.dart` is an unchanged snapshot of the user-supplied
`gios040Xsr_demo/lib/screen/pru/pru_protocol.dart` used for golden packet parity
tests. Keeping this fixture makes tests runnable from a clean PC repository
checkout without requiring the full Flutter project.
